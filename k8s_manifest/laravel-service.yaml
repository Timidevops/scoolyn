apiVersion: v1
kind: Service
metadata:
  name: laravel-service
  namespace: scoolyn
  labels:
    app: laravel-app
spec:
  selector:
    app: laravel-app
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
  type: ClusterIP